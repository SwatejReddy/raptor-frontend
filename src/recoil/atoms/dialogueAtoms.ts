import { atom } from "recoil"

export const isFollowersOpenAtom = atom({
    key: "isFollowersOpenAtom",
    default: false,
});

export const isFollowingOpenAtom = atom({
    key: "isFollowingOpenAtom",
    default: false,
});

