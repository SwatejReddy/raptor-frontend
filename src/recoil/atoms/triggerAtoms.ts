import { atom } from "recoil";

export const triggerProfileDataFetchAtom = atom({
    key: "triggerProfileDataFetch",
    default: false
})

export const triggerRippleDataFetchAtom = atom({
    key: "triggerRippleDataFetch",
    default: false
})